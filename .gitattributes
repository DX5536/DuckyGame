*.psd filter=lfs diff=lfs merge=lfs -text
*.png filter=lfs diff=lfs merge=lfs -text
*.odt filter=lfs diff=lfs merge=lfs -text

# Prevent line-ending conversion on the FastScriptReload package so git stops
# emitting "LF will be replaced by CRLF" warnings for its .meta / .cs files.
Assets/FastScriptReload/** -text
